//TODO Immutability means not changing the original data, but instead creating a new copy with modifications.

// so for this we may use the map function or the filter function which return s the new array
